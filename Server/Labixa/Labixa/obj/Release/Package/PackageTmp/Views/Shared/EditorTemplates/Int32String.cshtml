@model String
@if (Model != "Default")
{
    @Html.TextBox("", Int32.Parse(Model), new { @class = "col-xs-12 col-md-8", @type = "number" })
}
else
{
    @Html.TextBox("", 0, new { @class = "col-xs-12 col-md-8", @type = "number" })
}